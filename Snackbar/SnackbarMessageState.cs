namespace Snackbar
{
    public enum SnackbarMessageState
    {
        None,
        Queued,
        FadingIn,
        Visible,
        FadingOut,
        Completed,
        Removed,
        ActionPerformed
    }
}
